{
  "version": "2.0.0",
  "configurations": [
    {
      "name": "MysteryPayload (Build and Debug)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/mystery_debug",
      "MIMode": "gdb",
      "miDebuggerArgs": "--nx",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build_task"
    }
  ]
}